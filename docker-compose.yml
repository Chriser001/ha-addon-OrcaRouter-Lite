services:
  api:
    build: .
    container_name: orcarouter-lite
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - lite-data:/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////data/orca.db

volumes:
  lite-data:
